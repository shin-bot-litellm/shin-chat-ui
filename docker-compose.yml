version: "3.8"

services:
  shin-chat-ui:
    build: .
    ports:
      - "3001:3000"
    environment:
      - OPENCLAW_URL=${OPENCLAW_URL:-http://host.docker.internal:3000}
      - OPENCLAW_TOKEN=${OPENCLAW_TOKEN}
    restart: unless-stopped
